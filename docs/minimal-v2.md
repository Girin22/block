# 미니멀 V2

개발 서버 전용 대량 기록 테스트는 `?stress=600` / `1200` / `3600` / `172800`으로 열 수 있다. 현재 저장 규칙과 측정은 [고해상도 한 장 저장](highres-export.md), 이전 측정은 [긴 영수증 테스트](receipt-stress.md)를 참고한다. 프로덕션과 일반 진입에서는 활성화하지 않는다.

세션 한도는 초록 블록 172,800개(24×60×60×2)다. 흰 자동 타일은 한도를 소모하지 않는다. 실제 플레이 시간이나 입력 빈도를 측정하는 규칙이 아니라 0.5초당 한 개를 24시간 놓은 양으로 정한 개수 제한이다. 한도 도달 후 활성 타일과 실루엣을 숨기고 입력·자동 낙하를 막는다. 일시정지 메뉴에서 Resume는 비활성화하고 Export로 기존 기록을 저장할 수 있다. 저장 후 새 세션에는 한도를 다시 적용한다.

영수증 헤더는 12px 크기의 날짜·조각 수 한 줄이며, 바로 아래에 은은한 스티치 점선(3/4)을 유지한다. 미리보기와 저장 이미지에 동일하게 적용한다.
조각이 0개라면 숫자를 생략하여 `YYYY.MM.DD · piece`로 표시한다.

새 세션은 게임 화면을 처음 터치할 때까지 자동 낙하를 시작하지 않는다. 블록이 아닌 빈 게임 화면에서도 조작할 수 있다. 첫 터치 이후에는 다음 타일도 자동으로 낙하하며, 누르고 있는 동안 멈춘다. 메뉴 터치는 시작으로 취급하지 않는다. 테스트용 게임 조작 키도 시작 입력으로 인정한다. 새로고침과 Export 저장 후 새 세션에서는 다시 첫 터치를 기다린다.

입력 개선: 게임 화면 어디서든 탭으로 회전하고 스와이프로 이동한다. 12px 이상 움직이고 한 축 이동이 다른 축의 1.25배 이상이면 해당 제스처를 가로 또는 세로로 고정한다. 세로 제스처는 좌우 이동 없이 아래로만 내리고, 가로 제스처는 높이를 유지한다. 방향을 바꾸려면 손을 떼고 다시 스와이프한다. 회전은 현재 칸, 왼쪽 한 칸, 오른쪽 한 칸 순서로 빈 공간을 검사하며 위로 들어 올리거나 충돌을 무시하지 않는다.

빠른 안착 판정은 아래 방향 순이동 48 CSS px 이상이며 세로 이동이 가로 이동보다 클 때만 인정한다. 기존 75% 높이 조건도 함께 적용한다. 작은 드래그나 좌우 조정 후에는 현재 높이에서 일반 자동 낙하를 이어간다. 자동 낙하 자체에는 스와이프가 필요하지 않다.

저장 이미지에는 상단 중앙에 `YYYY.MM.DD · 00조각` 한 줄만 작은 글씨로 표시한다(2026-09-22까지는 `00 piece`). 조각 수는 흰 타일 포함 전체 개수이며 최소 두 자리로 표시한다. 브랜드·장식·저장 안내 문구는 제거하고 화면의 닫기/이미지 저장 동작은 유지한다. 저장 실패 시에만 오류 안내를 표시한다.

기존 소스와 데이터는 PC에 그대로 보존한다. Git 업로드와 기본 빌드에는 V2 진입점 `index.html`만 포함한다. `/legacy/`, V1 소스/패키지/테스트는 Git에서 제외하며 V2는 이 파일들을 참조하지 않는다.

게임판은 폭 9칸이다. 굴곡진 긴 인터로킹 블록 한 종류를 90도 회전하고, 세로 또는 가로 두 칸을 차지한다. 공유된 곡선 경계를 회전 대칭으로 만들어 모든 방향에서 맞물린다. 게임 화면을 누른 채 스와이프하며 위로 이동할 수 없다. 누르고 있는 동안 자동 낙하를 멈추고, 손을 놓으면 원위치 복귀 없이 현재 높이에서 낙하를 이어간다. 바닥 기준 75% 선 아래에서 의도적인 아래 스와이프로 내려놓으면 빠르게 안착한다. 탭 회전은 최대 한 칸의 좌우 보정을 포함해 충돌하지 않을 때 허용한다. 현재 높이의 실제 점유 칸으로 충돌을 판단하여 덮개 아래 열린 틈새에도 옆에서 들어갈 수 있다. 활성 블록은 초당 1.1칸 자동으로 내려가고, 바닥 접촉 0.45초 후 고정된다. 일시정지 중에는 하강·고정 타이머가 멈춘다. 이미 놓인 블록의 물리 중력·줄 지우기·게임 오버는 없다. 빈 한 칸의 상하좌우가 모두 막히면 흰 타일을 자동 생성한다. 주변 블록 종류나 대각선은 무관하고, 벽과 활성 바닥도 막힌 면으로 취급한다. 두 칸 이상 연결된 빈 공간은 채우지 않는다.

조작 반응: 세로 드래그 중에는 손의 이동량에 따라 높이를 내린다. 카메라는 입력·착지 중에도 속도가 연속적인 감쇠 추적으로 부드럽게 이동한다. 조작 중 활성 블록은 카메라 이동량을 보정하고 활성 바닥 상승은 멈춘다. 빠른 안착은 남은 거리에 따라 100~380ms 동안 점점 가속하며 착지 직전에 감속하지 않는다. 착지 시 기존 효과음을 재생한다(눌림·복원 효과는 2026-09-21에 제거, 아래 기록 참고). 일반 자동 낙하 속도와 75%/48px 안착 판정은 유지한다. 일시정지 메뉴의 두 버튼은 배경·글자·호버 색을 동일하게 사용한다.

카메라는 쌓인 높이를 화면 중간에 유지한다. 활성 블록은 카메라 상단에서 시작한다. 화면 아래를 지난 곳에는 블록을 떨어뜨릴 수 없도록 활성 바닥을 올린다. 지나간 메시와 충돌 셀을 해제하되, 실제 배치 목록은 이번 플레이의 영수증 출력을 위해 메모리에 보관한다.

V2는 브라우저 저장소를 읽거나 쓰지 않는다. 화면을 완전히 나가거나 새로고침하면 초기화하고, BFCache로 되돌아와도 새로 시작한다. 단순 탭 전환은 일시정지한다. Export는 거리 없이 실제 블록 배치 전체를 가상 스크롤 영수증으로 보여준다. 미리보기가 열리면 파일을 준비하고 사진 저장 클릭에서 기기 메뉴를 연다. iPhone에서는 사용자가 ‘이미지 저장’을 선택한다. 메뉴 API 완료 후 초기화하며 취소·실패 시 유지한다. API 완료는 실제 사진 앱 저장 완료를 보장하지 않는다. HTTP LAN 등 메뉴 미지원 환경에서는 PNG를 길게 눌러 저장한 뒤 ‘저장 완료’로 초기화한다. SVG는 명확한 파일 저장 안내와 버튼을 제공하며 다운로드 요청 후 초기화한다. 브라우저는 실제 파일 저장 완료를 알려주지 않는다. 미리보기를 닫으면 세션을 유지한다.

PNG는 축소 없이 2배 배율(가로 688px)로 유지하며, 256행씩 그려 하나의 PNG 파일로 압축한다. 전체 비트맵을 만드는 캔버스를 생성하지 않는다. 3,200만 픽셀 또는 세로 65,536px 초과, 압축 API 미지원, PNG 생성 실패 시에는 SVG 한 장으로 전환한다. SVG 윤곽은 defs/use로 재사용하며 최대치 기록도 모든 개별 타일의 위치와 색을 유지한다. 파일 생성 도중 미리보기를 닫으면 작업을 취소한다. iOS 실기기 사진 저장/최대 확대 지원 범위는 별도 확인이 필요하다. 효과음 합성은 `apps/play/src/audio.ts`에 두며 V1 설정/소스에 의존하지 않는다(소리 자체는 2026-09-21에 교체, 아래 기록 참고).


2026-09-21 에셋 변경: 가로 블록은 `assets/images/horizontal.png`, 세로 블록은 `vertical.png`, 흰 자동 채움 타일은 `center.png`를 사용한다. 회전 후에도 각 방향 이미지의 원래 명암 방향을 유지한다. 착지 실루엣과 Export 미리보기·PNG·SVG에도 동일 에셋을 적용한다. SVG는 세 이미지를 data URI로 한 번씩 내장하고 defs/use로 재사용하므로 저장 후 외부 파일에 의존하지 않는다. PNG는 원본 래스터 에셋이며 SVG로 저장해도 원본 이상의 디테일이 추가되지는 않는다.

2026-09-21 프레임·폭 변경: 보드 폭은 9칸이며 3칸짜리 맞물림 패턴 세 묶음을 나란히 놓을 수 있다. 개별 블록은 가로 2×1·세로 1×2를 유지한다. 화면은 어두운 배경(`#2c2929`)과 위에서 바로 내려다보는 카메라를 사용하며, 좌우·하단 프레임은 같은 블록 PNG를 격자에 맞춰 반복한다. 프레임은 장식이므로 조각 수·충돌·Export에 추가되지 않고, 스크롤 시 화면 주변 메쉬만 유지한다. 오른쪽 위 일시정지 버튼은 `assets/images/stop.png`를 사용한다.

낙하 블록 그림자는 원본 이미지의 한 칸 약 148px를 기준으로 X -26px·Y 40px·Blur 41px·검정 40%를 적용하고, 게임 화면 배율에 맞춰 축소한다. 가로·세로 알파 마스크를 한 번씩 생성해 재사용하며 매 프레임 블러를 계산하지 않는다. 착지점에 1.2칸 이내로 가까워지면 오프셋과 불투명도가 부드럽게 줄어들고 접촉 시 사라진다. 고정된 블록과 Export에는 낙하 그림자를 넣지 않는다. 보드 폭 변경 후에도 Export는 가로 688px를 유지한다.

2026-09-21 모바일·일시정지 변경: 휴대폰 세로 화면에서는 전체 가로 폭에 9칸 보드를 맞추고, 큰 화면에서는 최대 480px·화면 높이의 62% 폭으로 제한한다. 화면 크기가 달라져도 블록 비율과 입력 좌표가 일치하며, 진행 중 리사이즈 시 블록이 보드와 겹치지 않게 높이를 보정한다. 오른쪽 위 버튼은 안전 영역을 고려한 48px 터치 영역이다.

일시정지는 팝업을 열지 않는다. 낙하 블록·실루엣을 숨기고 쌓인 보도는 유지하며, 플레이 시간·초록 블록 수·흰 블록 수·Export가 아래에서 올라온다. 시간은 최초 게임 입력부터 세며 일시정지·Export·백그라운드 체류 시간은 제외한다. 거리 환산은 추가하지 않는다. 버튼은 `assets/images/Flow 2.lottie`를 정방향으로 재생해 고깔이 되고 재클릭하면 현재 프레임에서 역방향으로 재생해 일시정지 아이콘으로 돌아간다. Resume 글씨는 표시하지 않으며 모션 감소 설정에서는 끝 프레임만 표시한다. 플레이어와 WASM은 앱에 포함하고 외부 CDN에 의존하지 않는다.

Export만 기존 이미지 저장 모달을 연다. 닫기·Escape는 일시정지 화면으로 돌아오고, 저장 완료 시 새 세션으로 재개한다. 저장 취소·실패 시 배치를 유지하고, 세션 한도 도달 시 재개 버튼은 비활성화하되 Export는 사용할 수 있다.

2026-09-21 일시정지 모션 변경: 로띠 아이콘은 0.3초, 일시정지 화면 등장은 0.8초에 끝난다. 아이콘 배속은 고정값이 아니라 클립 길이를 0.3초로 나눠 계산한다(현재 약 3.1배속). Resume 글씨는 두지 않고 아이콘 모양만으로 상태를 구분한다. 통계와 Export는 64px 아래에서 올라오며, 투명도 520ms·이동 620ms의 감속 곡선과 60ms 간격의 순차 지연(0/60/120/180ms)을 사용해 마지막 항목이 800ms에 자리 잡는다. 재개할 때 화면은 220~280ms로 짧게 내려가 조작을 지연시키지 않는다. 모션 감소 설정에서는 전환을 생략한다.

2026-09-21 착지 표현 변경: 착지 예상 위치는 흐린 텍스처 대신 블록 이미지의 실제 윤곽을 추적한 점선(화면 기준 굵기 1.5px·선 5px·간격 4px)과 균일한 단색 채움으로 표시한다. 윤곽은 이미지 알파에서 한 번만 추출하고, 화면 크기에 맞는 해상도로 다시 그려 선이 번지지 않게 한다. 점선은 실루엣 안쪽에만 그려 이웃 블록을 덮지 않는다(`apps/play/src/landing-guide.ts`).

안착은 실제 보도블록처럼 단단하게 처리한다. 착지 후 눌림·복원 효과(이전 200ms)를 제거해 놓인 블록은 크기와 위치가 변하지 않는다. 대신 공중의 블록은 카메라에 가까운 만큼 최대 3.5% 크게 보이고, 착지점 1.2칸 이내에서 그림자와 함께 정확히 1배로 수렴해 접촉 순간 틈 없이 맞물린다. 가속 낙하(착지 직전 감속 없음), 100~380ms 길이, 75%/48px 판정, 효과음은 그대로다. 모션 감소 설정에서는 크기 변화를 생략한다.

2026-09-21 흰 채움 타일 변경: 크기가 커지며 튀어나오던 등장 효과(400ms, 오버슈트 포함)를 제거했다. 흰 타일도 초록 블록과 같은 방식으로 놓인다. 빈칸을 막은 블록이 안착하고 60ms 뒤, 빈칸 거의 바로 위(화면상 0.28칸)에서 나타나 200ms 동안 가속하며 내려와 감속·반동 없이 멈춘다. 높이는 화면상 이동이 아니라 바닥에 수직인 방향으로 표현한다. 전용 그림자가 최대 간격에서 0으로 줄어들고, 크기는 8%에서 정확히 1배로 수렴한다(처음에는 1.2칸 위에서 미끄러져 내려왔으나 멀리서 날아오는 느낌이라 같은 날 변경). 투명도는 낙하 초반 35% 구간에서만 올라가 접촉 순간에는 항상 불투명하다. 채움 효과음은 등장 시점(이전 90ms)이 아니라 안착 시점(260ms)에 재생한다. 수치는 `apps/play/src/drop-motion.ts`의 `FILL_DELAY`·`FILL_SECONDS`·`FILL_HEIGHT`·`FILL_LIFT`로 조절한다. 일시정지하면 진행 중인 채움은 즉시 안착 상태로 표시하고, 모션 감소 설정에서는 처음부터 안착 상태로 놓는다. 개발용 대량 기록을 불러올 때는 애니메이션 없이 배치한다.

2026-09-21 효과음 교체: 음정이 출렁이던 말랑한 두 소리(실리콘·젤리, 약 0.2초)를 없애고, 보도블록끼리 닿는 단단하고 건조한 소리 하나로 통일했다. 2.2kHz 대역의 22ms 접촉 잡음에 410Hz·985Hz·205Hz(130Hz로 하강) 공명음을 더한 합성음이며 길이는 약 0.04초다. 울림이 거의 없어 연속 배치에서도 겹치지 않는다. 흰 채움 타일은 같은 소리를 1.4배 높고 0.8배 짧고 0.75배 작게 낸다. 매번 ±2.5% 범위에서 음높이를 달리해 기계적인 반복을 피한다. 두 소리를 번갈아 내던 규칙은 제거했다. 재생 시점(블록 안착, 흰 타일 안착)과 출력 압축기 설정은 그대로다. 후보 비교 페이지는 git에서 제외된 `output/sound-samples.html`에 있다.

2026-09-21 시작 위치 변경: 모든 블록은 세로 방향으로 가운데 열(9칸 중 4번 열)에서 시작한다. 두 칸 폭의 가로 블록은 홀수 폭 보드의 정중앙에 올 수 없지만 한 칸 폭의 세로 블록은 정확히 중앙에 놓인다. `packages/play-core`의 `SPAWN_ROTATION`·`SPAWN_X`로 정의하며 새 세션·다음 블록·Export 저장 후 재시작에 모두 적용한다. 회전 순서와 벽 보정 규칙은 그대로다. 시작 위치를 가정하던 브라우저 검증 스크립트(`frame-shadow`, `pause-mobile`, `screen-input`, `local-play`)를 갱신했다.

2026-09-21 관리자 비 연출: 주소에 `?admin`이 있을 때만 게임 화면 바깥(왼쪽 아래 고정)에 ‘비 내림’ 토글을 표시한다. 휴대폰처럼 바깥 여백이 없는 화면에서는 같은 모서리에 겹쳐 표시한다. 일반 진입에서는 버튼도 날씨 레이어도 만들지 않는다. 비는 위에서 내려다본 시점으로 표현한다. 바람에 살짝 기운 빗줄기가 지면의 한 점에 도달하면 퍼지는 물결과 어두운 젖은 자국을 남긴다. 자국은 밝은 블록 위에서는 또렷하고 어두운 바닥에서는 거의 보이지 않으며 3.5~7초에 걸쳐 마른다. 비가 내리는 동안 블록과 프레임은 약 9초에 걸쳐 어둡고 차갑게 젖고(sRGB 기준 R 0.70·G 0.79·B 0.82 배), 바닥색과 화면 전체에도 은은한 푸른 어둠이 깔린다. 비는 2.5초에 걸쳐 강해지고 3.5초에 걸쳐 그치며, 완전히 그친 뒤 약 16초에 걸쳐 마른다. 전체가 젖을수록 개별 자국은 덜 도드라진다. 날씨는 게임 일시정지와 무관하게 계속 흐르고, 세션을 새로 시작하면 자국만 지우고 젖은 정도는 유지한다. 모션 감소 설정에서는 빗줄기·물결·자국 없이 젖은 색만 적용한다. 규칙·충돌·조각 수·Export에는 전혀 관여하지 않는다(`apps/play/src/rain.ts`).

2026-09-21 빗소리: 관리자 비 연출이 켜져 있는 동안 `assets/audio/rain-window.mp3`(1분 51초, 스테레오)를 반복 재생한다. 음량은 화면의 비 세기를 따라가므로 비가 강해지며 함께 커지고 그치며 함께 사라진다. 비가 완전히 그치면 재생을 멈춘다. 원본은 앞뒤 2초가 작아지도록 녹음돼 있어 단순 반복하면 회차마다 소리가 꺼진다. 그래서 두 개의 재생기를 두고, 끝나기 5초 전에 다음 회차를 처음부터 시작해 같은 음량(equal power)으로 교차시킨다. 전체를 디코딩하면 40MB가 넘으므로 스트리밍 재생기를 Web Audio 게인에 연결해 쓴다. 이 방식은 기기 음량만 조절되는 iOS에서도 페이드가 동작한다. 원본이 매우 작아(-39dBFS 부근) 2.1배로 키운다(처음 3배에서 30% 낮춤). 음원은 비를 처음 켤 때만 내려받으며 일반 진입에서는 요청하지 않는다. 게임 효과음과 별도의 오디오 컨텍스트를 써서 게임을 일시정지해도 빗소리는 이어지고, 탭을 떠나면 멈췄다가 돌아오면 이어진다. 수치는 `apps/play/src/rain-sound.ts`의 `LOOP_OVERLAP`·`RAIN_GAIN`으로 조절한다. 음원 출처와 라이선스(CC BY)는 README의 출처 항목에 적었다.

2026-09-22 한국어·글꼴·아이콘: 앱 안의 글은 모두 한국어다. `Export`는 `내보내기`, 플레이 시간의 `min`은 `분`으로 바꿨다. 글꼴은 영문(라틴 문자)만 Jost 가변 글꼴로, 한글·숫자·문장부호는 나눔손글씨 강부장님체로 표시한다. `unicode-range`로 Jost를 라틴 문자에만 묶어 두었으므로 숫자도 손글씨체로 나온다. 일시정지 통계는 참고 이미지처럼 큰 아이콘(세로 블록 56px, 흰 타일 36px, 그림자 포함) 아래에 손글씨 숫자를 놓는 세로 배치다. 일시정지 아이콘 로띠는 `assets/images/pause.lottie`(27프레임)로 교체했고 재생 길이 0.3초는 그대로다.

2026-09-22 내보내기 변경: 일시정지 화면의 ‘내보내기’는 팝업 없이 바로 저장한다. 결과는 배경이 투명한 RGBA PNG(가로 688px)이며 종이 배경·톱니·스티치 점선은 없다. 상단의 날짜·조각 수 한 줄은 SVG 텍스트가 아니라 캔버스에 손글씨체로 그린다(SVG 안의 텍스트는 웹 글꼴을 쓸 수 없기 때문). 저장을 누르면 버튼이 진행률을 보여 주고, 다운로드가 시작되면 새 세션으로 초기화하고 바로 재개한다. 실패하면 버튼 아래에 안내를 띄우고 기록을 유지한다. 크기 한도를 넘거나 PNG 생성에 실패하면 이전처럼 SVG 파일로 내려받으며, SVG도 배경이 투명하다. 브라우저는 다운로드 완료를 알려주지 않으므로 ‘취소 시 유지’는 더 이상 없다. 예전 영수증 미리보기 팝업(가상 스크롤·공유 메뉴·길게 눌러 저장)은 코드를 그대로 두고 `?receipt`가 있을 때만 연다. 배치 결과를 읽는 브라우저 검증 스크립트는 이 주소를 쓴다.

2026-09-22 제목: 브라우저 탭 제목은 ‘보도보도’다.

2026-09-22 빈 공간 채움 규칙 변경: 이전에는 상하좌우가 막힌 빈 한 칸만 흰 1×1로 채웠다. 이제 블록을 놓아 하늘로 통하는 길이 사라진 빈 공간은 크기와 관계없이 전부 채운다. 경계는 좌우 벽, 실제 바닥(y=0), 놓인 타일이며 카메라를 따라 올라가는 활성 바닥은 경계가 아니다. 그래서 충돌 셀 기록을 더 이상 지우지 않고 전체를 보관하며, 화면 아래로 지나간 공간도 위에서 닫히면 맨 아래까지 채운다(`Pavement.enclosedAround`, `flood`). 판정은 새 블록에 인접한 빈 칸에서 시작해 높은 칸부터 탐색하므로 열린 공간은 곧 하늘에 닿아 빨리 끝나고, 닫힌 공간만 전부 걷는다.

채움 타일은 흰 1×1(`center.png`), 2×1(`white_horizontal.png`), 1×2(`white_vertical.png`) 세 종류다. 두 칸짜리 원본(`fill_h.png`, `fill_v.png`)에는 그림자가 함께 그려져 있어 초록 블록과 같은 판에 맞도록 본체만 잘라낸 사본을 쓴다. 배치는 `tileRegion`이 정한다. 가장 아랫줄부터, 줄 안에서는 왼쪽부터 놓으며, 위와 오른쪽이 모두 비어 있으면 (x+y)의 홀짝에 따라 세로·가로를 번갈아 골라 이음새가 한 줄로 이어지지 않고 엮이게 한다. 한쪽만 비면 그쪽 긴 타일, 둘 다 막히면 1×1이다. 연출은 흰 한 칸과 같다. 같은 배치에서 생긴 타일들은 정해진 순서대로 200ms 간격(`FILL_STAGGER`, 처음 55ms에서 여유롭게 조정)으로 하나씩 내려앉고 각각의 속도는 같다. 효과음은 안착마다 울리되 한 번에 24개를 넘으면 건너뛰며 울려 총 24회를 넘지 않는다. 화면 밖에서 안착하는 타일은 소리를 내지 않는다. 흰 타일 수는 조각 단위로 세므로 2칸짜리도 1개다. Export의 SVG는 다섯 이미지를 내장하며 두 칸 흰 타일의 id는 `tile-2-1-white`·`tile-1-2-white`다.

2026-09-22 프레임 변경: 하단 프레임 줄은 실제 바닥(y=-0.5)에만 있고 카메라를 따라 올라오지 않는다. 화면이 올라가면 화면 아래쪽에는 사용자가 남긴 빈 공간이 그대로 보인다. 좌우 프레임은 그대로다.

2026-09-22 채움 타일 안착 연출: 흰 타일은 나타날 때 5°(`FILL_TILT`, 양수가 시계 반대 방향) 기울어져 있다가 자리에 끼워지며 똑바로 선다. 방향은 모든 타일이 같다(디자이너 확인 사항). 반대 방향이면 값의 부호만 바꾼다. 하강·회전 곡선은 디자이너의 Figma 인터랙션(Smart animate, Gentle, 800ms)을 따라 `cubic-bezier(0.44, 0, 0.56, 1)` 800ms로 바꿨다(이전 200ms 가속 낙하). 60ms 지연과 순차 간격(200ms), 그림자·크기 수렴, 안착 시점 효과음은 그대로다. 기울기 각도는 Figma 패널에 보이지 않아 임시값이며 디자이너 확인 후 조정한다.

2026-09-22 일시정지 통계 변경: 플레이 시간과 그 위의 블록 아이콘을 없애고, 맨 위에 아이콘 없이 깔린 길이를 미터로 표시한다. 그 아래는 초록 블록 아이콘과 개수, 흰 타일 아이콘과 개수다. 통계 영역은 화면 중앙(쌓인 길의 꼭대기 위치)보다 위에서 끝나도록 아래 여백을 54%로 두어 내보내기 글씨가 블록과 겹치지 않는다. 플레이 시간은 표시하지 않지만 측정은 그대로 한다.

미터 환산: 한 칸을 실제 I형 보도블록의 절반(225×112.5mm)인 11.25cm로 본다. 깔린 길이는 채워진 칸 수(흰 타일 포함)를 보드 폭 9로 나눠 11.25cm를 곱한 값이며, 빈 채로 남긴 칸은 세지 않는다. 소수 첫째 자리까지 표시한다(`CELL_METERS`, `pathMeters`, `formatMeters`). 이 환산은 임시 기준이며 만보기 연동 시 걸음 기준으로 바꿀 수 있다.

2026-09-22 모바일 대응: (1) 낙하 그림자가 휴대폰에서 검은 실루엣처럼 또렷하게 나오던 원인은 캔버스 `filter` 속성이다. iOS 사파리 18 이전과 일부 WebView는 이 속성을 조용히 무시해 블러가 빠졌다. 이제 시작 시 블러가 실제로 적용되는지 1픽셀로 검사하고(`canvasFilterWorks`), 안 되면 알파 채널을 소프트웨어 가우시안(박스 블러 3회, `blurChannel`)으로 흐려 같은 결과를 만든다. WebKit에서 재현과 수정을 확인했다. (2) 일시정지 글씨는 두 가지를 손봤다. 5.9MB TTF는 휴대폰에서 첫 일시정지 전에 다 받지 못해 시스템 글꼴로 보일 수 있어, KS X 1001 2,350자와 ASCII만 담은 0.6MB WOFF2 부분집합(`assets/fonts/GangBuJang-ksx1001.woff2`, `scripts/subset-font.py`로 재생성)으로 바꾸고 `font-display: block`과 시작 시 미리 불러오기를 적용했다. 집합 밖의 드문 음절은 시스템 글꼴로 대체된다. 또 `will-change`와 도착 후의 `translateY(0)`을 없애 요소가 합성 레이어에 머물지 않게 했다. iOS는 레이어에 남은 글자를 흐리고 고르지 않게 그린다. (3) `dvh`를 모르는 오래된 브라우저를 위해 `vh` 값을 먼저 두는 대체를 넣었다. 실제 기기의 글씨 문제는 데스크톱 WebKit에서 재현되지 않아 위 원인 추정에 따른 수정이다.
